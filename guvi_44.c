#include <stdio.h>

int main(void) {
         int input;    
    	 scanf("%d",&input);
	       if(input<=9&&input>=1)
	             printf("entered value with in the range ,no error");
	    	  else
                    printf("you entered number is not range");
	
	return 0;
}
